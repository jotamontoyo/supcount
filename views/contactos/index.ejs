<div class="container-fluid margen_superior" id="contactos">

	<h4><img src="/images/contacts.png" alt="" /> Contactos</h4>

	<div class="panel panel-default table-responsive center-block" id="contactos_panel">
		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th>Nombre</th>
					<th>eMail</th>

				</tr>
			</thead>
			<tbody>
				<% for (let i in contactos) { %>
					<tr>
						<td><%= contactos[i].nombre %> </a></td>
						<td><%= contactos[i].email %> </a></td>
<!--						<% if (session.user.isAdmin) {%>
							<td><a href="/contactos/<%= contactos[i].id %>/edit"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a></td>
							<td>
								<form method="post" action="contactos/<%= contactos[i].id %>?_method=delete">
									<button class="btn btn-danger btn-sm" type="submit" onClick="return confirm('Borrar: <%= contactos[i].nombre %>');">
										<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
									</button>
								</form>
							</td>
						<%}%> -->

						<% if (session.user) {
							if (session.user.isAdmin) {%>
								<td style="width:40px"> <a class="" href="/contactos/<%= contactos[i].id %>/edit">
									<img src="/images/edit3.png" style="width: 20px;"/>
								</a></td>
								<td style="width:40px">
									<form method="post" action="/contactos/<%= contactos[i].id %>/destroy?_method=delete">
										<button class="btn btn-danger btn-xs" type="submit" onClick="return confirm('Borrar el Contacto: <%= contactos[i].nombre %>');">
											<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
										</button>
									</form>
								</td>
							<%}%>
						<%}%>

					</tr>
				<% } %>
			</tbody>
		</table>
	</div>

	<% if (session.user.isAdmin) { %>
		<p><a href="/contactos/new"><button type="button" class="btn btn-primary">
			<i class="fa fa-plus" alt=""></i> Crear Contacto</button></a>
		</p>
	<%}%>

</div>
